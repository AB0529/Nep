import { Client as ClientJS } from "discord.js";
import { LoadClass, IDiscordParams, ICommandParams } from ".";
import { IClientOptions } from "./Types/ClientOptions";
export declare class Client extends ClientJS {
    private _silent;
    private _loadClasses;
    private _loadedOnEvents;
    private _loadedOnceEvents;
    get silent(): boolean;
    set silent(value: boolean);
    constructor(options?: IClientOptions);
    static setDiscordParams(discordInstance: InstanceType<any>, params: IDiscordParams): boolean;
    static setCommandParams(discordInstance: InstanceType<any>, instanceMethod: Function, params: ICommandParams): boolean;
    static getCommandsIntrospection(forPrefix?: string): import(".").IOn[];
    static getCommands<InfoType = any>(forPrefix?: string): import(".").ICommandInfos<InfoType>[];
    login(token: string, ...loadClasses: LoadClass[]): Promise<string>;
    private loadClasses;
}
